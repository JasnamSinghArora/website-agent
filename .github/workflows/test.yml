name: Website Report

on:
  schedule:
    - cron: "23,28,33,38,43,48,53,58 * * * *"
  workflow_dispatch:

jobs:
  run-report:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scheduled run works"
